@using FAMAndIMS.Data.Model.GlobalSettingModel.DepreciationModel
@model DepreciationTypeVM
<form id="saveDepreciationType" enctype="multipart/form-data" class="form-horizontal auth-form" method="post" autocomplete="off">
    <div class="card card-one">
        <ul>
            @if (ViewBag.Error is not null)
            {
                @foreach (var error in ViewBag.Error)
                {
                    <li class="text-danger">@error</li>
                }
            }
        </ul>
        <div class="card-body p-3">
            <div class="row">
                <div class="col">
                    <div class="mb-3">
                        <label for="DepreciationTypeName" class="form-label">DepreciationType Name<span style="color:firebrick">*</span></label>
                        <input asp-for="DepreciationTypeName" type="text" class="form-control" id="DepreciationTypeName" placeholder="">
                        <span class="text-danger" asp-validation-for="DepreciationTypeName"></span>
                    </div>
                    <div class="form-check form-switch">
                        <input asp-for="IsActive" class="form-check-input" type="checkbox" id="flexSwitchCheckDefault">
                        <label class="form-check-label" for="flexSwitchCheckDefault">Is Active</label>
                        <span class="text-danger" asp-validation-for="IsActive"></span>
                    </div>
                </div>
                <div class="col">
                    <div class="mb-3">
                        <label for="DepreciationTypeCode" class="form-label">DepreciationType Code<span style="color:firebrick">*</span></label>
                        <input asp-for="DepreciationTypeCode" type="text" class="form-control" id="DepreciationTypeCode" placeholder="">
                        <span class="text-danger" asp-validation-for="DepreciationTypeCode"></span>
                    </div>
                </div>
            </div>
        </div>
        <div class="modal-footer mt-3">
            <button type="button" class="btn btn-dark waves-effect" data-bs-dismiss="modal">Cancel</button>
            <button class="btn btn-primary" type="submit">Submit <i class="ri-send-plane-fill"></i></button>
        </div>
        <!-- card-body -->
    </div><!-- card -->
    </div>
</form>
<script>

    FormHandler.init({
        method: 'POST',
        actionUrl: '@Url.Action("SaveDepreciationType", "DepreciationType")',
        containerSelector: '#add-new .modal-body',
        selector: '#saveDepreciationType'
    }, function () {
        $("#add-new .modal-body").html("");
        $("#add-new .modal-title").html("");
        $("#add-new").modal('hide');

        Pagination.getData();
    });
    $(document).ready(function () {
        document.getElementById("flexSwitchCheckDefault").checked = true;
    })
</script>